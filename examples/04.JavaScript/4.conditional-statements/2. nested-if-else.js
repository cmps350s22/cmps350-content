let first = 5;
let second = 8;

if (first == second) {
	console.log(`${first} = ${second}`);
}
else {
	if (first > second) {
        console.log(`${first} > ${second}`);
	}
	else {
        console.log(`${first} < ${second}`);
	}
}